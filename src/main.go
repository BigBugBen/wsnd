package main

import (
	"fmt"
)

func main() {
	// http.HandleFunc("/", HelloServer)
	// http.ListenAndServe(":3030", nil)
	fmt.Println("ctmd " + `\`)
}

// func HelloServer(w http.ResponseWriter, r *http.Request) {
// 	fmt.Fprintf(w, "Hello, zzy's father!")
// }
